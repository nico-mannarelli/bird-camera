{
  "name": "bird-detection-app",
  "version": "1.0.0",
  "description": "Bird species detection app with Netlify Functions",
  "scripts": {
    "dev": "netlify dev"
  },
  "devDependencies": {
    "@netlify/plugin-python": "^1.0.0"
  }
}


# PyTorch is installed separately in Dockerfile for CPU-only version
# This reduces size from ~2GB to ~200MB

# ML and image processing
ultralytics
opencv-python-headless
pillow
gdown

# Web framework
flask
flask-cors
gunicorn

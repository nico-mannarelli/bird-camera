# Use CPU-only PyTorch (much smaller, ~200MB vs ~2GB+ with CUDA)
# Install PyTorch CPU version from PyTorch's CPU index
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
torchvision==0.24.1+cpu

# ML and image processing
ultralytics
opencv-python-headless
pillow
gdown

# Web framework
flask
flask-cors
gunicorn
